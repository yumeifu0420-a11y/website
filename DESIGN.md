# May Editorial Studio — Design System

## 1. 视觉主题与氛围

独立杂志式的个人工作室：清浅纸面、手绘黑线、少量清透冷蓝。气质应当灵动、好奇、有人情味；主视觉用“大留白 + 文字远景 + 极小人物”形成诗意尺度，不做企业插画或 AI 科技视觉。

## 2. 色板与角色

- Canvas `#f7f8f5`：页面主底色
- Paper `#ffffff`：需要干净阅读的表面
- Ink `#1d2327`：主文字，不用纯黑
- Muted `#5f6a70`：次级文字
- Hairline `rgba(29,35,39,.16)`：分隔线
- Current Blue `#5f93ad`：链接、焦点与小面积强调
- Pale Blue `#d9e8ee`：选中、标签和氛围

## 3. 排版规则

- 中文正文使用系统中文栈，16–18px，行高 1.75。
- 展示标题使用 `Iowan Old Style / Baskerville / Georgia` 与中文系统宋体回退，字重 500，不斜体。
- UI 与导航使用中文系统无衬线，字重 500。
- H1 使用 `clamp(3rem, 7.6vw, 7.6rem)`，最多 3 行。

## 4. 组件样式

- 主要按钮为 Ink 实底、Paper 文字、8px 圆角；次要动作以文本链接呈现。
- 内容分组优先使用留白和 1px hairline，不堆卡片。
- 项目行在桌面采用不等比编辑网格，移动端折叠为单列。
- 图片使用 0 或 8px 圆角，只在交互和实物边界需要时出现容器。

## 5. 布局原则

- 最大宽度 1240px，桌面 48px 边距，移动端 20px。
- Hero 以大幅插画为底，文字悬在上半部留白中；人物与文字地景停留在下半部，形成上下尺度差。
- 服务、项目、方法以章节式长页面展开，不使用三等分卡片。

## 6. 深度层级

页面整体扁平。仅导航和悬停项目使用 Notion 式 4 层低透明度阴影：`rgba(29,35,39,.04) 0 4px 18px, rgba(29,35,39,.025) 0 2px 8px`。

## 7. Do / Don't

- Do：大留白、强裁切、真实作品链接、职责标签、细线、克制动效。
- Don't：泛黄滤镜、女人走路、叶子便签、蓝紫渐变、玻璃拟态、卡片墙、左侧竖线、斜体。
- 插画语法：可以借鉴墨绿水流、钴蓝窄幅影像、颗粒印刷、手绘数据曲线与非对称微型文字；参考图不直接进站，只转译其构图与质感。

## 8. 响应式

768px 以下变为单列；导航简化但保留项目联系入口；图片显式尺寸、首屏主图优先加载；所有可点击目标至少 44px。

## 9. Motion 哲学

动效只解释流动与反馈：进入视口时 8–14px 轻移 + 透明度，按钮按压 0.98，项目行 hover 2px。全部使用强化 ease-out，仅动 transform / opacity，并提供 reduced-motion 兜底。
